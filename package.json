{
  "name": "geodraft-conversion-service-rcp",
  "version": "1.0.0",
  "description": "RCP pre-processor for AWS Batch: RCP → E57 via CloudCompare, then triggers the standard E57 conversion job",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "@aws-sdk/client-batch": "^3.0.0",
    "@aws-sdk/client-s3": "^3.0.0",
    "@aws-sdk/lib-storage": "^3.0.0",
    "dotenv": "^16.0.0"
  }
}
